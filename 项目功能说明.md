# VirtuCoach-Graduate 项目功能说明

> AI 吉他陪练系统 —— 通过"音频 + 视频 + 大模型"三路信号，自动识别弹奏中的错误并给出针对性指导。
> 技术栈：FastAPI (Python) + SQLite + 原生 HTML/CSS/JS 前端；核心依赖 basic-pitch、MediaPipe、librosa、DeepSeek API、Qwen-VL API、ChromaDB。

---

## 一、系统整体架构

```
前端多页面（原生 HTML/CSS/JS，深色音乐主题）
   │  HTTP / SSE 流式
   ▼
FastAPI 后端（backend/，端口 1218）
   ├─ routers/     路由层：参数解析、响应格式化（auth/analysis/hand_check/chat/practice/analytics/references）
   ├─ services/    业务编排：分析流水线、手型检查服务、报告后处理
   ├─ pipeline/    手型问题过滤管道（多阶段降噪去重）
   ├─ 核心算法：audio_analyzer（音频）/ video_processor（视频）/ chord_analyzer（逐弦）/ vision_analyzer（视觉AI）
   ├─ deepseek_agent（AI 老师）/ audio_diagnosis（诊断引擎）/ guitar_tab_assigner（品位分配）
   ├─ db/          SQLite 数据层（用户/练习记录/反馈/参考图）
   └─ knowledge/   知识库（和弦/技巧/曲目 markdown）
```

**数据流向**：用户上传/录制演奏视频 → 音频轨提取 → 音符转录与和弦识别 → 视频关键帧手型检测 → 视觉 AI 复核 → 过滤管道去噪 → AI 老师生成报告 → 返回前端并（登录用户）自动存入练习记录。

---

## 二、用户页面功能

### 1. 落地页（index.html）
- 品牌宣传 + 4 个功能卡片（音频分析 / 手型检测 / AI 报告 / 进步追踪）。
- 登录态感知：已登录显示"进入控制台"，未登录可"游客体验"直接进入分析页。

### 2. 用户系统（register.html / login.html）
- **注册**：用户名唯一性实时校验（防抖）、密码强度要求（≥8 位含大小写+数字）、确认密码一致性。
- **登录**：JWT 认证；**单会话机制**（见 3.5）。Token 存 sessionStorage（每浏览器窗口隔离）。
- 密码用 bcrypt 哈希存储，不可逆；后端固定 JWT 密钥保证重启不失效。

### 3. 控制台（dashboard.html）
- **统计卡**：练习总次数 / 平均分 / 最高分 / 最近 7 天次数（后端统计）。
- **练习趋势**：最近 10 次得分柱状图（纯 CSS 渲染）。
- **练习历史**：分页表格（15 条/页），按模式过滤，分数三档颜色徽章（<60 / 60–80 / ≥80）。
- **双记录对比**：勾选两条记录弹窗对比分数差异，高亮 ↑↓。
- **报告查看**：展开 AI 报告、删除记录、导出 PDF（新窗口打印）。

### 4. 练习分析页（analysis.html）——核心页面
两种模式，支持 URL 直达（`?mode=hand`）：

#### 4.1 视频分析模式（自由演奏分析）
1. 上传或摄像头录制（≤30s），可选水平/变调夹。
2. 异步分析：后端任务队列，前端四步进度条（上传→音频→手型→AI）轮询更新，显示各阶段耗时。
3. 结果展示：
   - 综合 / 音准 / 节奏 / 手型四维分数（数字动画）。
   - **原视频回放**：错误时间点可点击跳转（手写 206 Range 流式，HEVC 视频自动用 H.264 代理回放）。
   - 检测详情双 Tab（音频问题 / 手型问题，严重度三色）。
   - **正确手型参考对比图**（按当前和弦/问题自动匹配参考图）。
   - **AI 老师报告**（Markdown 渲染，含评分锚定与章节强制覆盖）。

#### 4.2 手型检查模式（和弦检查 / 技巧检查）
- 和弦下拉列表（来自知识库扫描）+ 技巧下拉（左右手 optgroup 分组，含勾弦/泛音/制音/扫弦等）。
- 5 秒录制或上传 → **逐弦音频清晰度分析 + 手型检测 + 视觉 AI 对比 + AI 综合评估**。
- 结果：正确参考图 VS 用户截图并排对比、逐弦音色结果、逐指对比表、问题列表、AI 总结与练习建议。

#### 4.3 AI 聊天（侧栏精灵）
- SSE 流式回答；基于当前分析结果上下文 + 知识库 RAG + 最近 10 轮历史。
- 支持报告内容**划词提问**；含曲目推荐（按和弦重叠匹配曲库）。

#### 4.4 反馈意见（悬浮按钮）
- 分类 / 严重度 / 描述 / 截图（点击、拖拽或 Ctrl+V 粘贴），自动关联当前登录用户。

### 5. 游客策略
- 未登录也可完成全部分析功能；**登录后分析结果自动存入练习记录**，并享受历史追踪。

---

## 三、核心功能与实现方法

### 3.1 音频分析（audio_analyzer.py）

| 功能 | 方法 |
|---|---|
| **音符转录** | Google **basic-pitch** 神经网络，从波形直接预测 MIDI 音符事件（起止时间/音高/力度）。后处理：过滤 <0.08s 或力度<18 的噪声伪影；**八度泛音去重**（同时刻±50ms、相差 12/19/24 半音的音符只留力度大的） |
| **演奏起始/节拍** | 起始检测 + 节拍跟踪（估计 tempo BPM），供节奏评分与时间段定位 |
| **和弦识别** | **librosa chroma_cqt** 求 12 维 chromagram → 中值滤波去噪 → 与内置和弦模板做**余弦相似度匹配**（含根音能量排名惩罚、C 大调 diatomic 加分、七和弦验证、capo 移位感知）。再经**多源证据链**：音符反向验证 → 易混淆和弦消歧（如相对大小调）→ 和弦驱动音符过滤（滤掉不属于任何和弦 pcset 的"幻音"，同时自然屏蔽人声音符） |
| **扫弦检测** | onset cluster 检测 + 频谱特征（频谱质心斜率 / 高频尾比 / 峰值密度）分类单音 vs 扫弦；扫弦模式识别（上行/下行方向）；**扫弦质量五维评估**（节奏 / 力度 / 方向 / 漂移） |
| **技巧检测** | 音符模式 + 频谱特征识别：击勾弦（legato）、泛音（harmonics）、制音（palm mute）、扫弦（strumming）时间段 |
| **节奏评分** | IOI（音符间隔）量化到节拍网格，检测抢拍/拖拍 → 节奏准确率 |
| **音准评分** | 错音计数（规则 + DeepSeek 辅助判定）→ 音准准确率 |
| **录音质量评估** | RMS 信噪比判定 good/weak/poor，质量差时提示重录 |

### 3.2 手型检测（video_processor.py）

| 功能 | 方法 |
|---|---|
| **手部关键点** | **MediaPipe Hands**，每帧提取 21 个手部关键点（2D+3D），区分左右手 |
| **关键帧采样** | 均匀采样（上限 200 帧）+ 音频错误时间点加成采样，密集捕捉亚秒级手型问题 |
| **角度计算** | 3D 关键点 → 计算关节弯曲角（MCP/PIP 等），用"内角"约定衡量手指弯曲程度 |
| **左手问题检测** | 拇指位置（只测位置不判紧度）、各指弯曲弧度（太直/太弯）、手腕弧度、手指竖直度等，**带和弦上下文**（按当前和弦的标准角度对比，Phase 2b） |
| **右手问题检测** | 扫弦手腕运动轨迹、力度均匀度 |
| **拍摄角度自适应** | 通过手腕关键点判断正面/侧面拍摄，正面用更低的弯曲阈值（几何失真补偿） |
| **HEVC 代理转码** | ffprobe 探测，遇 HEVC/高分辨率/高帧率视频先用 ffmpeg 转 720p30 H.264 代理再分析，避免 OpenCV seek 超时；代理顺带留存供前端回放 |

### 3.3 手型检查（hand_check_service.py + chord_analyzer.py）

| 功能 | 方法 |
|---|---|
| **和弦知识库** | 从 `knowledge/chords/*.md` 加载指法（每弦品位如 [0,2,2,0,0,0]）、标准角度、练习要点 |
| **逐弦音频清晰度** | 从视频提取音频，逐弦检测 onset + 频谱 → 判定每根弦是否发声、音色是否清晰（SNR 前置评估，质量差则全部标不确定） |
| **手型检测 + 视觉复核** | MediaPipe 手型问题 + Qwen-VL 参考图对比（见 3.4） |
| **AI 综合评估** | DeepSeek 按和弦/技巧标准打分（0–10 分制）并给出问题与练习建议，交叉验证音频结果与手型结果 |

### 3.4 视觉 AI 分析（vision_analyzer.py）
- 视觉模型：**Qwen-VL-Max**（默认）/ Claude。
- **多图参考对比**：Qwen-VL 支持单消息多图，把正确手型参考图放前面、用户截图放后面，让模型做"有参照"的对比分析。
- **MediaPipe 上下文注入**：把关键点角度数据写进 prompt，引导模型关注关节细节。
- **RAG 参考图检索**：参考图预存 21 点关键点向量（63 维），按余弦相似度检索最接近的"正确手型"范例。

### 3.5 认证与单会话机制（auth.py + user_service.py）
- **JWT**（HS256，24h 过期）：token 内含 user_id/username/session_version。
- **单会话拦截（先到者优先）**：`users.online` 列 + 90s 心跳新鲜度判断——账号在线且 90s 内有心跳则拒绝新登录；前端每 30s 心跳，页面关闭发 offline 信标。配合 session_version 递增踢掉旧窗口 token（sessionStorage 隔离）。
- 关键坑已处理：纯布尔 online 在浏览器崩溃时会卡死，故叠加 last_activity 新鲜度兜底。

### 3.6 AI 老师（deepseek_agent.py）
- **分级报告生成**：beginner / intermediate / advanced 三套专属 system prompt；结构化 JSON 输出；含"AI 审核驳回"清单（驳回的问题报告不得再提）。
- **RAG 知识检索**：知识库用 **bge-small-zh embedding** 向量化入 **ChromaDB**；生成报告前做**三轮检索**（问题文本语义检索 → 问题类型关键词精确匹配 → 练习方法检索），缺依赖时降级关键词匹配。
- **流式聊天**：SSE，基于任务上下文 + RAG + 10 轮历史。
- **曲目推荐**：解析 `recommended-songs.md`，按用户水平和当前和弦**和弦重叠排序**推荐曲目。
- **防线**：禁猜曲目名铁律、不答越权问题、输出端过滤弦/品引用。

### 3.7 报告后处理（report_postprocessor.py + pipeline/sanitizer.py）
- **弦/品引用过滤**：AI 输出全文正则扫描，`X弦X品`、音名+八度（如 G3）等替换为占位符——解决"AI 幻觉品位"问题（输出端一次过滤，源头 prompt 改不干净）。
- **章节强制覆盖**：手型/音频章节按检测结果确定性重写，防止 AI 编造"上次表现"等幻觉内容。
- **评分锚定**：音频分 =（音准+节奏）/2，手型分用确定性规则计算，报告中的评分与实际分数对齐。
- **占位符清理**：移除残留的 `[品位待核实]` 等占位符。

### 3.8 品位分配（guitar_tab_assigner.py）
- **DP/Viterbi 算法**：把 MIDI 音符序列映射到最优"弦 + 品位"（6 弦，O(N×36)）。含空弦奖励、手移动代价、跨弦代价、和弦跨度硬约束（≤4 品）、甜区（低把位）偏好；时间感知（间隔>2s 不施加移动代价）；手区偏置（按快速手部估算的位置约束品位）。

### 3.9 音频诊断引擎（audio_diagnosis.py）
- 从信号特征产出**结构化诊断**：节拍稳定性、逐音符节奏问题定位、动态/力度、音色、杂音、音准错音、扫弦质量七类；每个发现分**红/黄/绿**三档严重度，供报告与用户快速定位问题。

### 3.10 数据层（backend/db/）
- SQLite 多库：`users.db`（用户 + login_events 登录日志）、`practice_sessions.db`（练习记录）、`feedbacks.db`（反馈）、`references.db`（参考图元数据 + 63 维关键点向量）。
- **性能优化**：`PRAGMA synchronous=NORMAL`（WAL 推荐值），登录等写操作从 ~400ms 降到几十 ms。

---

## 四、管理员页面功能（admin.html）

入口为密码门禁（`/api/verify-admin`），含 **5 个标签页**：

### 1. 数据概览
- 5 个统计卡：用户总数 / 今日新增 / 练习总次数 / 今日练习 / 反馈总数。
- Chart.js 折线+柱状图：近 14 天登录趋势、练习趋势（CDN 加载失败降级为表格）。

### 2. 意见收集（反馈管理）
- 状态统计卡（待处理/已知悉/处理中/已解决/今日新增）+ 多条件筛选（分类/状态/关键词防抖）。
- 列表展开详情：查看反馈截图、浏览器信息自动解析。
- 操作：标记处理完成 / 已知悉 / 重新打开 / 改状态 / 存管理员备注 / 删除。

### 3. 用户管理
- 用户表格：用户名/邮箱/密码掩码/注册时间/上次登录/**在线状态（绿点，基于 90s 心跳新鲜度）**/累计在线时长/练习次数。
- **重置密码**（bcrypt 不可逆，只能重置不能查看）。

### 4. 参考图管理
- 上传图片 → **AI 自动分析**（Qwen-VL 提取手型/技巧/身体部位等元数据）预填 → 保存（自动计算 63 维关键点向量）。
- 画廊缩略图浏览 + 删除；向量缺失时批量补算。

### 5. 数据分析（毕业展示重点）
- **在线统计**：实时在线（online=1 且 90s 有心跳）/ 今日活跃 / 近 7 日活跃 / 在线时长 Top10。
- **登录频率**：近 N 天登录分布。
- **练习分析**：练习量 / 用户排名 / 曲目分布 / 分数段统计。
- **用户增长**：每日新增 + 累计曲线。
- **使用意愿评分**：30 天窗口，登录频率 30% + 练习频率 30% + 累计在线秒 20% + 反馈次数 20%，各维度全用户 min-max 归一化加权，分高/中/低三档。
- **CSV 导出**：utf-8-sig（带 BOM，Excel 免乱码）下载。

---

## 五、主要技术点汇总

| 领域 | 技术/方法 |
|---|---|
| 音符转录 | basic-pitch 神经网络 |
| 手部跟踪 | MediaPipe Hands（21 关键点） |
| 和弦识别 | librosa chroma_cqt + 模板余弦匹配 + 多源证据链验证 |
| 逐弦分析 | 逐弦 onset + 频谱清晰度判定 |
| 手型问题判定 | 关键点角度阈值（拍摄角度自适应 + 和弦上下文） |
| 视觉 AI | Qwen-VL 多图参考对比 |
| AI 老师 | DeepSeek 分级 prompt + RAG（bge + ChromaDB） |
| 品位分配 | DP/Viterbi 动态规划 |
| 认证 | JWT + 单会话（online 列 + 心跳 + session_version） |
| 回放 | 手写 206 Range 流式 + H.264 代理 |
| 报告可信度 | 弦/品引用输出端过滤 + 章节强制覆盖 + 评分锚定 |
| 数据 | SQLite 多库 + 参考图向量 RAG |

---

*生成日期：2026-08-18 · 基于当前 F:/VirtuCoach-Graduate 代码库梳理*
