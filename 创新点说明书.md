# VirtuCoach-Graduate 项目创新点说明书

> 面向软件工程专业毕业设计的创新点阐述。配套文档：《项目功能说明.md》（系统全貌与实现细节）。
> 本文先回答"做算法还是做别的"这一定位问题，再逐条给出可答辩的创新点。

---

## 零、定位：先回答导师的问题

导师问"是做算法还是做别的"，本质是在判断这份毕业设计的**创新点落在哪个层面**。软件工程专业的毕业设计通常分三类：

| 类型 | 评判标准 | 本项目是否匹配 |
|---|---|---|
| 算法研究型 | 提出全新算法并在公开基准上超越 SOTA | ❌ 不匹配（basic-pitch/MediaPipe/chroma/Qwen-VL/DeepSeek 均为现成能力） |
| **系统开发 + 应用创新型** | 用工程手段解决一个真实、有难度的问题，且方案有巧思 | ✅ **主体匹配** |
| 纯 CRUD 业务系统 | 只有增删改查，无技术含量 | ❌ 本项目远高于此 |

**一句话定位**：本项目是**"面向吉他陪练的多模态 AI 诊断系统"**，创新主体在**应用创新 + 工程创新**，辅以**若干面向具体问题的算法改进**（可单独答辩）。

**给导师的答法**：
> "主体是应用和工程创新——我把音频、视频、大模型三路信号做交叉验证融合，解决'弹错什么→为什么错→怎么改'的闭环，这是现有吉他 App 没有的；算法层面我没去发明全新模型，而是针对吉他这个具体领域做了几个可验证的方法改进（和弦多源证据链、品位分配动态规划、扫弦匹配法、LLM 幻觉控制），这几个可以单独展开。"

---

## 一、创新点总览

| # | 创新点 | 层面 | 一句话概括 |
|---|---|---|---|
| 1 | 三路信号交叉验证的融合诊断架构 | 工程/架构 | 音频+视频+大模型不是简单拼接，而是互为证据、互相约束 |
| 2 | 领域化 LLM 幻觉控制（报告可信度工程） | 工程 | 用输出端过滤+章节强制覆盖+评分锚定，把 AI 编造压到最低 |
| 3 | 和弦识别"多源证据链"+ 和弦驱动音符过滤 | 算法 | 用和弦反验音符，并借和弦掩码自然屏蔽人声干扰 |
| 4 | 品位分配 DP/Viterbi 算法 | 算法 | MIDI 音符→最优"弦+品位"映射，带手部运动学约束 |
| 5 | 扫弦"排除法→匹配法"重构 + 质量五维评估 | 算法 | 从"排除非扫弦"改为"匹配扫弦证据"，方向检测更稳 |
| 6 | 参考图向量化检索（把 RAG 思想引入视觉参考） | 数据工程 | 21 关键点→63 维向量，检索最接近的"正确手型"做对比 |
| 7 | 面向真实设备的鲁棒性工程 | 工程 | HEVC 代理转码、Range 流式回放、SQLite 性能优化等细节 |

---

## 二、创新点详述

### 创新点 1：三路信号交叉验证的融合诊断架构（核心）

**做了什么**：系统同时采集 **音频轨**（音符/和弦/扫弦/节奏）、**视频轨**（MediaPipe 手部关键点/角度）、**大模型**（DeepSeek 诊断 + Qwen-VL 视觉复核），并把三者**交叉验证**，而不是各自输出再拼接：

- 音频识别出的**和弦** → 反过来约束**手型标准角度**（按当前和弦比对，Phase 2b）；
- MediaPipe 的**确定性角度判定** → 由 **Qwen-VL 视觉 AI 二次复核**（参考图对比）；
- **AI 老师**生成报告时，交叉验证"音频结果"与"手型结果"是否一致再下结论；
- 音频诊断出的**错误时间点** → 反过来驱动**视频关键帧加密采样**，密集捕捉亚秒级手型问题。

**为什么新**：现有吉他练习 App（Yousician、Fender Play 等）基本是"跟弹打分"（音准/节奏），或"视频教学播放"，**没有**把"音频证据 + 视觉手型 + 大模型诊断"做成互相约束的闭环。本项目的融合不是多模态拼盘，而是**信号互为证据链**。

**证据（代码）**：`video_processor.py`（音频错误点加成采样）、`hand_check_service.py`（和弦上下文 + 视觉复核）、`deepseek_agent.py`（音频/手型交叉验证）、`services/analysis_service.py`（流水线编排）。

---

### 创新点 2：领域化 LLM 幻觉控制（报告可信度工程）

**做了什么**：大模型生成诊断报告时会"编造"（幻觉），典型如编造具体品位、编造"你上次表现退步了"。本项目用三道防线控制：

1. **输出端弦/品引用过滤**：全文正则扫描 `X弦X品`、音名+八度（如 `G3`）等，替换为占位符——因为源头 prompt 永远改不干净，改在输出端最稳；
2. **章节强制覆盖**：手型/音频章节按**检测结果的确定性**重写，AI 只能写"检测到了什么"，不能编"上次表现"等跨次比较；
3. **评分锚定**：报告中的评分与实际分数对齐（音频分 =（音准+节奏）/2，手型分用确定性规则计算），AI 不得自造分数。

**为什么新**：这是 **"LLM 落地到垂直领域的可信度工程"**，是当前工程界的热点难题（LLM 幻觉治理）。把"输出端过滤 + 结构性约束 + 数值锚定"组合成一个可复用的报告后处理流水线，有现实的工程参考价值。

**证据（代码）**：`pipeline/sanitizer.py`（弦/品过滤）、`report_postprocessor.py`（章节覆盖 + 评分锚定）。

---

### 创新点 3：和弦识别"多源证据链" + 和弦驱动音符过滤

**做了什么**：单靠 chroma 模板匹配识别和弦误判率高。本项目叠加一条**多源证据链**：

1. **模板匹配**：chroma_cqt → 中值滤波 → 与和弦模板余弦相似度（含根音能量排名惩罚、C 大调 diatonic 加分、七和弦验证、capo 移位感知）；
2. **音符反向验证**：用 basic-pitch 转出的音符去验证和弦 pcset 是否吻合；
3. **易混淆和弦消歧**：如相对大小调（C/Am）等易混对单独处理；
4. **和弦驱动音符过滤**：滤掉不属于任何已识别和弦 pcset 的"幻音"——这一步**顺带把弹唱时的人声音符自然屏蔽掉**（人声不构成和弦音）。

**为什么新**：第 4 点是巧思——**利用和弦的音高集合（pcset）做掩码，在"吉他+人声"混合音频里抑制人声干扰**，无需单独的人声分离模型（Demucs 等会破坏和弦检测，本项目实测已回退）。这是针对弹唱场景的一个轻量而有效的解。

**证据（代码）**：`audio_analyzer.py`（chroma 匹配 + 证据链 + 和弦驱动过滤）、`analysis_service.py`（containment_bonus 区间包含修复）。

---

### 创新点 4：品位分配 DP/Viterbi 算法

**做了什么**：把 MIDI 音符序列映射为吉他上的"最优弦 + 品位"组合。建模为动态规划（O(N×36)，6 弦 × 最高品位）：

- 状态 = 每个音符落在"哪根弦哪个品位"；
- 代价函数 = 空弦奖励 + 手移动代价 + 跨弦代价 + 和弦跨度硬约束（≤4 品）+ 甜区（低把位）偏好；
- **时间感知**：音符间隔 >2s 时不施加手移动代价（允许换把）；
- **手区偏置**：按快速手部估算的位置约束品位。

**为什么新**：这是本项目**最接近"算法创新"的一个点**——不是直接用现成库，而是把"音符→指法"这一吉他家特有的映射问题形式化成带运动学约束的最优化，并给出可运行的 DP 解。

**证据（代码）**：`guitar_tab_assigner.py`。

---

### 创新点 5：扫弦"排除法→匹配法"重构 + 质量五维评估

**做了什么**：

- **架构重构**：早期用"排除法"（排除非扫弦特征来判定扫弦），误判多；后重构为"匹配法"——**主动匹配扫弦的正向证据**（pitch ordering 方向检测、onset cluster、频谱特征），假阳性显著下降；
- **质量五维评估**：节奏 / 力度 / 方向 / 漂移 四个维度量化扫弦质量，力度采用 **U 型评分**（力度过大过小都扣分，最优在中间）。

**为什么新**：扫弦是吉他陪练里"最容易判错"的难点，从"排除"到"匹配"的范式转变 + 把"漂移""方向"这类老师才会关注的主观指标量化，是一个可复用的方法论。

**证据（代码）**：`audio_analyzer.py`（扫弦检测 + 五维评估）。

---

### 创新点 6：参考图向量化检索（RAG 思想引入视觉参考）

**做了什么**：把"正确手型"做成**参考图数据库**，每张图预存 21 个手部关键点 → **63 维向量**；诊断时把用户手型的关键点向量与参考图库做**余弦相似度检索**，取出最接近的"正确范例"做并排对比，并作为多图输入喂给 Qwen-VL 做"有参照"的视觉复核。

**为什么新**：把大模型领域的 **RAG（检索增强）思想从文本迁移到视觉参考**——不是让视觉模型"裸猜"标准手型，而是先检索再对比，提高了视觉判断的确定性和可解释性。

**证据（代码）**：`vision_analyzer.py`（多图对比 + 向量检索）、`db/reference_db.py`（63 维向量存储）。

---

### 创新点 7：面向真实设备的鲁棒性工程（软工特色）

这部分不追求算法新，但体现软件工程的"解决真实问题"：

- **HEVC 代理转码**：手机拍的 HEVC/高分辨率视频，OpenCV 逐帧 seek 会超时，先 ffmpeg 转 720p30 H.264 代理再分析，代理顺带供前端回放；
- **手写 206 Range 流式回放**：错误时间点可点击跳转（部分内容响应）；
- **SQLite 性能优化**：`PRAGMA synchronous=NORMAL`，登录等写操作从 ~400ms 降到几十 ms；
- **单会话机制**：`online` 列 + 90s 心跳新鲜度 + `session_version` 两套机制并存，解决"多端同时登录"与"崩溃后卡死无法登录"的矛盾。

---

## 三、与现有工作的对比

| 能力 | Yousician / Fender Play / JustinGuitar | 本项目 VirtuCoach |
|---|---|---|
| 音准/节奏跟弹打分 | ✅ | ✅（basic-pitch + IOI 量化） |
| 手型/姿势检测 | ❌（多为静态示范） | ✅ **MediaPipe + 参考图 + 视觉 AI 对比** |
| "为什么错、怎么改"的诊断 | ❌（只告诉你错了） | ✅ **AI 老师 + 知识库 RAG + 针对性练习** |
| 和弦/扫弦/技巧检测 | 部分（纯音频） | ✅ **音频+视频交叉验证** |
| 弹唱（吉他+人声）场景 | 弱 | ✅ **和弦 pcset 掩码自然屏蔽人声** |
| 报告可信度控制 | — | ✅ **幻觉三防线** |

---

## 四、答辩可能的追问与应对

**Q1：算法是你自己写的吗？还是调库？**
> 基础能力（音符转录、手部关键点、和弦模板）用了开源库；但**品位分配 DP、和弦多源证据链、扫弦匹配法、报告后处理流水线**是自己设计和实现的，这些可以直接展示源码。

**Q2：创新点是不是太"工程化"，算法含量不够？**
> 软件工程专业的毕业设计，核心评价是"能否用工程手段解决一个真实且有难度的问题"。本项目的问题（多模态融合诊断、LLM 幻觉、弹唱人声干扰、指法映射）都是真问题，且有可验证的方案，这比"套一个模型跑个公开数据集"更符合软工培养目标。

**Q3：和纯算法研究（如 ISMIR 论文）比，你的贡献在哪？**
> 定位不同：论文追求单点 SOTA，本项目追求**端到端可用的陪练系统**。我的算法贡献是"面向具体问题的改进"，工程贡献是"把多个弱信号交叉验证成可信诊断"。

**Q4：怎么证明有效？**
> 有回归测试套件（和弦/扫弦/评分三套，带基线视频，位于 `tests/`，`scripts/run_regression_tests.bat` 一键运行），管理员面板有使用意愿评分、练习趋势等数据可佐证系统的可用性。

---

## 五、一句话总结

> **本项目用"音频+视频+大模型"三路信号的交叉验证融合，构建了能指出"弹错什么、为什么错、怎么改"的吉他 AI 陪练系统；创新主体在应用与工程层面（融合架构、LLM 幻觉控制、鲁棒性），辅以品位分配 DP、和弦多源证据链、扫弦匹配法等可单独答辩的算法改进。**

---

*生成日期：2026-08-20 · 面向软件工程专业毕业设计答辩*
